export enum AppRoute {
  Login = '/login',
  Favorites = '/favorites',
  Offer = 'offer/',
  Room = ':id',
  Root = '/'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknow = 'UNKNOW'
}
